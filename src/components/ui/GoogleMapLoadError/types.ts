export type GoogleMapLoadErrorProps = {
  errorLoadMap: {
    msg: string;
    locationLinkText: string;
    locationLink: string;
  };
  containerStyle: {
    width: string;
    height: string;
  };
};
